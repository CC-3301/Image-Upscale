# 64: `tests/` 区域的四条小尾巴（webp 中转 / 夹具命名 / 脚手架复用 / 格式判据）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（四条全部收口，见 Comments）

> *This was generated by AI during triage.*

## 缘起

批次 59-63 的 r2 / r3 评审记录项。按 `AGENTS.md`「小问题不单开票、不单跑轮」的处置，攒批登记，
按**同一区域（`tests/`）**归成一批，将来正好要动这块测试基建时一次闭环。四条都不影响测试能测出什么。

## 四条内容

1. **webp 夹具的 `.tmp.png` 中转还剩两份**：`tests/test_naming.py` 早前一处、`tests/test_sr.py` 各一份，
   与工单 63 删掉的真 JPEG 那一份同形。应收敛到 `tests/conftest.py` 的既有 helper。
   （工单 63 的 AC 只点名「真 JPEG 夹具」，故未在票内做。）
2. **`conftest.make_gray_jpg` 命名与用法不匹配**：它现在被当通用 JPEG 夹具用（工单 63），名字里的
   `gray` 只是它的初始用途。要么改名，要么在 docstring 里写清「灰度，仅供结构型断言」。
3. **新增的直通缩放用例又自建了一份脚手架**：`tests/test_naming.py` 里新用例的
   `mkdir` → `make_png` → `_reference_product` → `run_engine([... --no-rename])` 与 `_run_in_place` 同形，
   只差夹具尺寸与 `--width`。可给 `_run_in_place` 加 `size=` / `args=` 两个带默认值的参数收回这一份；
   注意别为单一调用者开一堆参数（反向也是 smell），收不收由维护者定。
4. **两条原地覆盖用例缺 `lessons §1.8` 的格式判据**：大写扩展名例、西里尔例只有 `size` 断言，
   没有 `img.format`（同区域另两例已有）。既有行，非工单 63 引入。

## Acceptance criteria

- [x] `tests/` 内不再有自建 `.tmp.png` 中转的 webp 夹具（实测 `rg "tmp\.png"` 在 `tests/` **0 命中**，比票面「只剩 conftest 一处」更彻底）
- [x] `make_gray_jpg` 的名字或 docstring 与其实际用途一致（改名 `make_jpg` + docstring 写明「灰度内容，只供结构型断言」）
- [x] 原地覆盖的脚手架在 `tests/test_naming.py` 内只有一处（`_run_in_place`，四个调用点全走它）
- [x] 四条原地覆盖用例都带 `lessons §1.8` 的「退出码 + 尺寸 + 格式 + PSNR」四项
- [x] `python -m pytest tests -q` 全绿（100 passed），用例数与改动前一致，末尾无「用例被跳过」告警

## 未决 / 风险

- 纯整洁度，无用户可见影响；第 4 条是判据厚度，不是覆盖缺口（内容正确性已由 PSNR 兜住）。
- 改测试基建时注意 `lessons §3.12`（搬助手别把紧邻的 `@needs_engine` 一起删掉）。

## Comments

### 批次收口（2026-09-20，批次 64+65+66 一次闭环）

- **交付（7 个 commit）**：`ad98cb5`（四条主改）→ `dcbbc3a`（夹具 helper 去重：webp 双重编码、jpg 中转）→ `1ae5de7`（docstring 口径 + 断言块收成 `_assert_in_place_product`）→ `9ecc6fe`（`make_png` docstring 口径）→ `92fbc6f`（`test_exit_codes` 注释口径）→ `73c586d`（删死赋值）→ `aa4a71d`（删无消费者的 `return img` 与失真 docstring）。
- **四条落实**：① 两处 webp `.tmp.png` 中转 → `conftest.make_webp`，全 `tests/` 内 `.tmp.png` 归零；② `make_gray_jpg` → `make_jpg`（docstring 写明灰度、只供结构型断言）；③ 直通缩放用例改走 `_run_in_place`（新增带默认值的 `size=` / `args=`），`_reference_product` 不再钉产物名；④ 四条原地覆盖用例齐「退出码 + 尺寸 + 格式 + PSNR」，断言块收成 `_assert_in_place_product`。
- **同形夹具评估后不收敛（有理由，非遗漏）**：`make_rgba`（alpha 合成语义）、`_make_fine_png`（细网点同时是 PSNR 参考源）、`make_sync_fixture`（RNG 图块 + 返回 path）、`_sparse_dots`（返回 numpy、不落盘）——内容本身就是断言的一部分，强行合并等于换夹具。
- **两轴轨迹**：r1 双 approved（5 P2）→ fix → r2 双 approved（3 P2）→ fix → r3 双 approved（2 P2）→ fix → r4（注释级 2 P2）→ fix → r5 收敛轮（spec 无新项，standards 报 1 处死赋值）→ fix → **r6 双 approved**。
- **gate**：引擎无待编译改动 + `dotnet build gui` 0 警告 0 错误 + `pytest tests -q` **100 passed**，无跳过告警；缺引擎路径另验「15 skipped 而非 error」（lessons §3.12）。
- **遗留观察已收口**（`aa4a71d`）：`_assert_in_place_product` 无消费者的 `return img` 与 docstring 半句已删（维护者 2026-09-20 定：这类也算小问题，当轮收）；随后两轴复审（按新立的「评审收敛线」只报 P0/P1 与新增 P2）双 approved。
- **AC 口径说明**：票面 AC1 括号写「`rg "tmp\.png"` 只剩 conftest 一处」，实际做到 0 命中（更强）。
