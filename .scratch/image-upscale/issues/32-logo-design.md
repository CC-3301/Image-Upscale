# 32: 设计软件 LOGO，出三版供选择

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（IU 定稿 v1 已接入；V2/V3 备选可随时切换重打）

> *This was generated by AI during triage.*

## 报告（维护者 2026-09-15 迭代需求 8）

软件目前没有 LOGO，设计一个，出三版让维护者选。

## 方案要点

- 用脚本（PIL）程序化绘制 3 版设计稿（PNG 预览 + ico 就绪的多尺寸画布 256/128/64/48/32/16），输出到 `.scratch/image-upscale/logo/`
- 设计方向（围绕"超分/放大/保真"意象，避开 AI 生成式涂抹联想）：
  1. **放大镜 + 上行箭头**：直白表达"放大"，圆角方底现代扁平风
  2. **像素阶梯**：小格到粗格的阶梯式过渡，表达分辨率层级攀升，深底亮色
  3. **层叠图片 + 升箭头**：多图层堆叠表达图像处理，暖色活力
- 维护者选定后：转 .ico 接入 exe 图标（csproj `ApplicationIcon`）+ 窗口图标，随后打 v0.2.2 包
- 选定稿落库 `gui/assets/`（源 PNG + ico），三版提案全稿留在工单目录备查

## Acceptance criteria

- [ ] 三版设计稿 PNG 可直接查看，256px 下图形清晰、16px 不糊
- [ ] 选定稿生成合法 .ico（多尺寸）并接入 exe/窗口图标

## Comments

### 提案记录（2026-09-15）
- 三版设计稿与可复现脚本见 `.scratch/image-upscale/logo/`（PNG 预览 + 多尺寸 ico）：
  1. `proposal-1-magnifier.png` —— 放大镜 + 上行箭头，靛紫渐变（"放大"直白意象）
  2. `proposal-2-pixels.png` —— 像素阶梯攀升，深蓝底青绿渐变（分辨率层级意象）
  3. `proposal-3-layers.png` —— 层叠照片 + 升箭头，深炭底琥珀暖色（图像处理意象）
- 均按 16px 可辨识标准绘制（粗轮廓高对比）；选定后接入 csproj ApplicationIcon + 窗口图标，并随 v0.2.2 打包

### 定稿轮记录（2026-09-15）
- 维护者否掉三版抽象提案，定调：**直接用 IU（Image Upscale 缩写）当主题**
- 新三版（`.scratch/image-upscale/logo/`，脚本 `make_logo_iu.py`）：`iu-v1-flat`（靛紫渐变白字）/ `iu-v2-glow`（深蓝底青翠渐变字）/ `iu-v3-light`（浅底紫字描边框）
- **v1 作为默认接入**：`gui/assets/app.ico`（csproj ApplicationIcon = exe 文件图标）+ Resource pack URI（窗口/任务栏图标）；ExtractAssociatedIcon 验证 exe 图标非空
- V2/V3 如需切换：替换 `gui/assets/app.ico` 重打即可
- 验证轮反馈（2026-09-15）：IU 字母偏小 → 字号改为按目标宽度 760/1024（约 74%）自适应，三版同步放大；v1 已重打进 v0.2.2 包
