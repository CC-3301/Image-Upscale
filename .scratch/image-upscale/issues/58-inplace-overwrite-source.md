# 58: 「文件添加扩展名 = 关」且输出格式与输入相同时，改为**原地覆盖源文件**（撤销拒绝写盘守卫）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（代码与构建完成；GUI 原地覆盖行为待人工验收）

> *This was generated by AI during triage.*

## 报告（维护者，2026-09-20 v0.2.9 发布前）

对工单 50 定案 4 的「拒绝写盘」不认可：这种情况下**要求覆盖**，「改代码重新构筑」。

## 维护者定案（2026-09-20）

- **撤销**工单 50 定案 4 的拒写守卫。关闭「文件添加扩展名」且输出格式与输入扩展名相同时
  （`A.png` + `-f png --no-rename`）目标路径即输入路径 → **直接写盘、原地替换源文件**。
- 语义回到 spec 用户故事 18「输出与已有文件同名时直接覆盖」，**不再有例外**。
- 不新增二次确认、不新增界面提示、不备份源图（维护者定案接受「源图被替换且不可撤销」）。
- 工单 50 的其余定案全部不变：`--no-rename` 只作用于文件输入；文件夹输入固定添加后缀、忽略开关。

## 方案要点

- `engine/src/engine_core.cpp`：删除 `is_same_path_ci`（约 `:670`）与 `run_files` 里的
  `if (single_file && is_same_path_ci(outpath, inpath)) { fail(...); continue; }` 分支（约 `:886`）。
  `CompareStringOrdinal` 只存在于被删的 `path_equal_ci` 内，随之删除。
  **`setlocale(LC_ALL, "")` 保留**（编排者 2026-09-20 订正：原文写的「`setlocale` 相关引用随之清理」
  是笔误）——它早于工单 50 就存在，影响 `wcstod` 解析 `-s` 的小数点等进程 locale 行为，不是本票死代码。
- **原地覆盖的安全性依据（实施者需在代码注释里写明并核对）**：本文件在解码阶段一次性读入内存并
  `fclose`（`engine_core.cpp:730-750`），写盘发生在推理之后（`:1076-1089`），中间不再读输入路径
  → 原地写不会读到半写状态。若核对发现存在第二次读取输入路径的路径（含 alpha / 直通缩放分支），
  必须在证据里报告并停下讨论，不得默默改成"先写临时文件再替换"。
- `gui/MainWindow.xaml.cs`：删除 `refuse to overwrite input: ` 的日志映射（约 `:520-521`，将成死代码）。
- 测试（`tests/test_naming.py`）：把三条「拒绝写盘」用例改成「原地覆盖」用例——
  `A.png` + `-f png --no-rename` → 退出码 **0**、`A.png` 路径不变、内容/尺寸为超分结果（夹具 32×32 → 64×64）、
  目录内**不新增**文件；含非 ASCII 文件名一例（原西里尔用例改写）。
- 文档：`spec.md:78`（命名规则段）、`spec.md:82`（冲突策略段）、`README.md:84`、`CONTEXT.md:48`
  里的「拒绝写盘 / 绝不覆盖源图」表述 → 改为「原地覆盖源文件（源图被替换）」。

## Acceptance criteria

- [ ] 关 + 同格式：`A.png` + `-f png --no-rename` → 退出码 0，`A.png` 被替换为超分结果，目录内无新文件
- [ ] 关 + 异格式：`A.png` + `-f jpg --no-rename` → 新增 `A.jpg`，`A.png` 字节不变（行为不变）
- [ ] 开（默认）：命名全表与 v0.2.8 逐字一致（既有 `test_naming.py` 用例不改断言即证）
- [ ] 文件夹输入 + 该开关 → 仍输出到带后缀目录、忽略开关（工单 50 定案 1 不回归）
- [ ] `refuse to overwrite input` 文案在引擎与 GUI 中均无残留（`rg` 零命中）
- [ ] 非 ASCII 文件名一例原地覆盖通过
- [ ] gate 全绿（引擎构建 + GUI 构建 + `python -m pytest tests -q`）

## 未决 / 风险

- **源图不可恢复**：覆盖即替换，无备份、无提示（维护者定案接受）。
- 目录批处理不受影响（开关对文件夹不生效）。
- 发布面：v0.2.9 尚未 push/tag/Release，本票改动并入同一版本，不新起版本号；
  `dist/v0.2.9/Image-Upscale-win64.zip` 需在代码定稿后重新打包。

## Comments

> *This was generated by AI during triage.*

### 实施记录（2026-09-20，v0.2.9 发布前）

- **改了什么**：删引擎守卫（`is_same_path_ci` / `path_equal_ci` + `run_files` 的 refuse 分支）+ GUI 日志映射；
  `tests/test_naming.py` 三条「拒写」用例改为「原地覆盖」（含 **PSNR ≥ 40** 内容判据与源图字节断言）；
  `spec.md:78/82`、`README.md:84`、`CONTEXT.md:48` 四处口径改为「**产物路径与输入路径相同 → 原地覆盖源文件**」
  并补命中（`A.png`/`A.PNG` + `-f png`）与不命中（`A.jpeg` + `-f jpg` → 新增 `A.jpg`）例。
- **两轴轨迹**：r1 双 `approved`（3 P2）→ fix r2 → r2 双 `approved`（4 P2）→ fix r3 → **r3 双 `approved`**（4 P2 全 report-only）。
- **编排者裁定留痕**：① 票面原文「`setlocale` 相关引用随之清理」是编排者笔误 → 裁定 **只删守卫与 `CompareStringOrdinal`，保留 `setlocale`**（早于工单 50 存在，决定 `wcstod` 解析 `-s`），票面已订正（`63bfbed`）；② 评审在 diff 里看到的 `docs/lessons.md` 与票面 hunk 是编排者的单独提交（`973a9a9` / `63bfbed`），不属本票交付物。
- **安全性依据（评审独立复核成立）**：输入唯一读取点 `engine_core.cpp:686`，读满即 `fclose`（`:712`）→ 写盘在推理/直通缩放/alpha 合成之后（`:1030/:1038/:1043`）
  中途无第二次读输入路径的通路；`probe_denoise_level`（`:391`）仅目录分支调用。故原地写不会读到半写状态。
- **gate**：`.pi-implement\gate.cmd` → **PASS，98 passed**（编排者复跑三次：43s / 44s / 40s）；`pytest -k no_rename` → 7 passed。
- **残余风险**：源图不可恢复（无备份/无确认）；覆盖写盘**非原子**（中途断电/崩溃会留下截断的源图）；
  「直通缩放 + 关后缀 + 同格式」与 `A.jpeg` 不命中边界目前只有手工探针证据（无自动化用例）。
- **候选（未做）**：三条覆盖用例脚手架去重；`engine_core.cpp:841` 注释措辞与四处文档统一；为 `A.jpeg` 边界与直通缩放分支补用例。
- **提交**：`d80b5c9` → `62daf44`（r2）→ `12b6957`（r3）；**未 push**。
