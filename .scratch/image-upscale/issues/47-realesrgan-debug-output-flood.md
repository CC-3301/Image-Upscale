# 47: Real-ESRGAN 遗留调试输出刷屏 stderr（[DBG] ×3 + 逐图块百分比）

**Parent:** .scratch/image-upscale/spec.md

**Category:** bug

**Status:** fixed（已入工作区，编译通过；未跑测试、未发布）

> *This was generated by AI during triage.*

## 报告（全量代码审查，2026-09-18）

`engine/src/realesrgan.cpp` 改前留有四处无条件调试输出：

- 逐图块 `fprintf(stderr, "%.2f%%\n", ...)`（`:533`）
- `fprintf(stderr, "[DBG] m2 geom %dx%d\n", tile_w, tile_h); fflush(stderr);`（`:618`）
- `fprintf(stderr, "[DBG] m4 padded %dx%d\n", in_f32.w, in_f32.h); fflush(stderr);`（`:650`）
- `fprintf(stderr, "[DBG] m5 extracted %dx%d\n", out_f32.w, out_f32.h); fflush(stderr);`（`:659`）

GUI 把无法识别的 stderr 行**原样**写进日志面板（`gui/MainWindow.xaml.cs:386-397`），所以这些行会直接刷在用户的日志框里：

- 违背工单 08 的「逐文件汇总」与「全程仅中文」；
- 与已文档化的 stdout 进度行 `progress d/t`（`engine_core.cpp:652-655`）重复，且 stderr/stdout 两套进度并存；
- `-g -1`（AGENTS.md 规定的测试后端）下必现。
- 同时违反 `docs/lessons.md` §1.3「输出统一走回调/统一编码，禁止绕过输出面直接打印」。

## 已做（工作区，未构建未验证）

删除上述 4 行。已核对 `tests/` 与仓库文档中**没有任何**对 `[DBG]` 或 `%.2f%%` 的引用（唯一进度契约仍是 stdout 的 `progress d/t`）。

## 回归守卫

`tests/test_models.py::test_realesrgan_cpu_path_has_no_debug_output`（断言 stderr 无 `[DBG]`、无 `%`，且进度仍走 stdout 的 `progress 1/1`）

## 备注

`docs/lessons.md` §1.3 的同类残留还有一批（模型加载失败时直写 stderr 的 `_wfopen %ls failed`，见 `waifu2x.cpp` / `realcugan.cpp` / `realesrgan.cpp` 的 `load()`，以及死代码 `filesystem_utils.h:45`），本次未动 —— 它们要在「路径输出统一 UTF-8 + 走回调」的整批整改里一起处理。

## Comments

- 2026-09-18 AI：全量代码审查中标准轴 lane 与规格轴 lane 各自报出，已复核到源码行级。
