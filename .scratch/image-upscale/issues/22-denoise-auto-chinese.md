# 22: 降噪档位显示"AUTO"应为"自动"

**Parent:** .scratch/image-upscale/spec.md

**Category:** bug

**Status:** done（代码与构建完成）

> *This was generated by AI during triage.*

## 报告（2026-09-14 人工测试）

降噪选项 UI 写"自动"，不要写 AUTO。

## Triage 验证记录（代码确认）

`MainWindow.xaml.cs OnModelChanged`：`DenoiseBox.Items.Add("AUTO")`。违反 spec US25"我希望 GUI 全程只有中文"——按规格属缺陷而非口味问题。

## 分诊建议

推荐 `ready-for-agent`（trivial）：`"AUTO"` → `"自动"`。GUI 内部逻辑用的是 `SelectedIndex`（0 = 自动档）不受影响；引擎参数 `--denoise auto` 不变。全局搜一遍其余 "AUTO" 文案（错误提示、日志）确保无漏网。

## Comments

### Agent Brief（2026-09-14 推进 ready-for-agent）

**Category:** bug
**Summary:** 降噪档位显示"AUTO"违反"全程中文"规格，应为"自动"

**Current behavior:**
降噪下拉第一项显示"AUTO"，违反产品规格的用户故事 25（GUI 全程只有中文）。

**Desired behavior:**
降噪档位全部以中文呈现："自动/无/低/中/高"；内部档位索引逻辑与引擎参数（auto）不变。

**Acceptance criteria:**
- [ ] 降噪下拉显示"自动"
- [ ] GUI 全界面无 "AUTO" 字样残留（含错误提示与日志文案，全局检查）
- [ ] 选择"自动"时引擎仍收到 auto 语义参数，行为不变

**Out of scope:**
- 引擎 CLI 参数名变更（CLI 保持 auto/none/low/mid/high）
- 工单 23/24 的启用逻辑与默认值变更

### 实施记录（2026-09-14）
- "自动/无/低/中/高"；SelectedIndex 逻辑不变，引擎参数仍 auto
