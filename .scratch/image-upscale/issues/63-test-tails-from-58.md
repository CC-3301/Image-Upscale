# 63: 工单 58 留下的测试尾巴（脚手架去重 + 真 JPEG 夹具复用 + 直通缩放分支补用例）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（三条全做；AC2 口径已限定为「真 JPEG 夹具」）

> *This was generated by AI during triage.*

## 缘起

工单 58 的 r2 / r3 / r4 评审记录项。按维护者 2026-09-20 新立的「「小尾巴」构筑前先问维护者」流程
（`AGENTS.md`），当时决定不为了它们再跑一轮实施 + 评审 + 打包，留到下一版。三条都在
`tests/test_naming.py`，一起做比分开做便宜。

## 三条内容

1. **三条原地覆盖用例共用同一套脚手架**（三份同形：`folder = workdir / "B"` → `mkdir` → `make_png` →
   `_reference_product(...)` → `run_engine([..., "--no-rename"])` → 断言）。抽一个模块内 helper
   （如 `_run_in_place(workdir, name)`），三条用例各留 3~4 行；或参数化覆盖 `A.png` / `A.PNG` 两例。
   注意：三例的断言差异（一例跑两轮 32→64→128、一例专验大写扩展名、一例非 ASCII）是刻意的，别抹平。
2. **真 JPEG 夹具的 `A.tmp.png` 中转是第 3 份拷贝**（另见 `tests/conftest.py` 的 `make_gray_jpg`、
   `tests/test_naming.py` 早前一处）→ 复用既有 helper，删掉第三份。
3. **「直通缩放 + 关后缀 + 同格式」分支无自动化用例**（r3 spec 轴报；`spec.md` 已承诺该组合原地覆盖）。
   补一条：64×64 的 `A.png` + `--width 32 --no-rename -f png` → 断言退出码 0、目录内**仅有** `A.png`、
   尺寸变 32×32。**这条不跑模型（直通缩放），秒级完成**，属便宜的好测试。
   非空过性验证：把守卫/命名改坏（例如让该组合落到带后缀的产物名）→ 用例必须红。

## Acceptance criteria

- [x] 三条原地覆盖用例的重复脚手架消失（收敛为 `_run_in_place`）
- [x] 真 JPEG 夹具只有一份实现（`conftest.make_gray_jpg`），真 JPEG 的 `.tmp.png` 中转只剩这一份 —— 口径限真 JPEG；`tests/` 内另有两处 **webp** 中转（`test_naming.py` 早前一处、`test_sr.py`），票面未列入，转候选票 64
- [x] 新增「直通缩放 + 关后缀 + 同格式」用例，且非空过性已验证（改坏则红），另按 lessons §1.8 补齐格式与 PSNR 内容判据
- [x] `python -m pytest tests -q` 全绿（本批终态 100 条），会话末尾无「用例被跳过」告警

## 未决 / 风险

- 第 1、2 条不影响测试能测出什么，只是整洁度；第 3 条是真覆盖缺口（边界很窄：同时满足
  "缩小图 + 关后缀 + 格式相同"）。
- 改测试基建时注意 lessons §3.12（搬助手别把紧邻的 `@needs_engine` 一起删掉）。

## Comments

### 批次收口（2026-09-20，批次 59 + 60 + 61 + 62 + 63 一次闭环）

- **交付**：`6e77903`（三条全做）+ `3cb5dee`（r3：新用例补格式与 PSNR 内容判据，对齐 `lessons §1.8` 的守卫口径）。
- **三条落实**：① 三条原地覆盖用例收敛到 `_run_in_place`，三例刻意的断言差异（两轮 32→64→128 / 大写扩展名 / 非 ASCII 双拼写）全部保留；
  ② `.jpeg` 用例的 `A.tmp.png` 中转删除，改用 `conftest.make_gray_jpg`；③ 新增
  `test_no_rename_direct_resize_same_format_overwrites_in_place`（64×64 + `--width 32 -f png --no-rename`，不跑模型，~0.15s）。
- **非空过性（真改守卫，不是改测试）**：`single_file_outpath` 的 `if (no_rename)` 临时改成 `no_rename && !direct_resize` + 重链 → 用例**红**
  （目录多出 `A-(Resize)-32x.png`、源图仍 64×64）→ 复原 + 重链 → **绿**。`lessons §3.12` 守卫也验过：
  `IU_ENGINE` 指向不存在的 exe → 15 skipped、无 error（`@needs_engine` 未在搬运中丢失）。
- **两轴轨迹**：r1 双 `approved` → r2 双 `approved` → **r3 双 `approved`**。
- **gate**：`pytest tests -q` **100 passed**（基线 99 + 本票 1 条），会话末尾无跳过告警；引擎/GUI 构建 0 新增警告。
- **候选（未做，转候选票 64，同在 `tests/` 区域）**：`test_naming.py` 早前一处与 `test_sr.py` 各有一份 **webp** `.tmp.png` 中转（与真 JPEG 那两份同形）；
  `make_gray_jpg` 名字带 gray 却当通用 JPEG 夹具用；新增直通缩放用例又自建了一份与 `_run_in_place` 同形的脚手架；
  大写扩展名 / 西里尔两条用例还缺 `lessons §1.8` 的格式判据。
