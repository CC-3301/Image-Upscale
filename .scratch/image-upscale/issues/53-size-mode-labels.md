# 53: 尺寸模式文案「指定宽 / 指定高」→「指定宽度 / 指定高度」

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（代码与构建完成；文案视觉按维护者 2026-09-20 定案默认通过，见 Comments）

> *This was generated by AI during triage.*

## 报告（维护者，2026-09-20，implement-pro 批次进行中）

尺寸模式那行的两个单选按钮文案要写全：「指定宽」→「指定宽度」，「指定高」→「指定高度」。

## 方案要点

- 只改两处 XAML 文案（`gui/MainWindow.xaml`）：

  | 位置 | 现在 | 改为 |
  |---|---|---|
  | `:64` | `Content="指定宽"` | `Content="指定宽度"` |
  | `:66` | `Content="指定高"` | `Content="指定高度"` |

- **不改**：`gui/MainWindow.xaml.cs:514` 的代码注释（`// 指定宽值`）、引擎 `--width/--height` 的英文
  usage、spec 里的模式名（`ratio | width | height`）、`setting.ini` 键（`LastScaleWidth/Height`）。
- 该行是 `WrapPanel`（工单 19：窄窗口自动换行），文案变长只影响换行点，不影响 `MinWidth`。

## 变更文件

- `gui/MainWindow.xaml`（两处 `Content`）。

## Acceptance criteria

- [ ] 两个单选按钮逐字为「指定宽度」「指定高度」
- [ ] 尺寸模式的功能行为不变：选中后仍传 `--width` / `--height`，`setting.ini` 键值不变
- [ ] 窗口最小尺寸（780×540）下不出现裁剪（该行 `WrapPanel` 允许换行，`MinWidth` 不变）
- [ ] 运行态实测记录该行在 780 宽下的实际占用与是否换行（写入证据）

## 未决 / 风险

- 文案 +2 字 ×2（约 +48px）：780×540 最窄宽度下该行可能由一行变为两行（`WrapPanel` 设计如此）。
  若维护者不接受换行，需另定（缩短文案/收窄两个输入框）——默认按允许换行实施。
- 「降噪与输出」行（工单 51）行宽余量仅 4px，本票不动该行；若后续再往该行加内容需重新实测。

## Comments

> *This was generated by AI during triage.*

### 实施记录（2026-09-20，implement-pro 批次 `feat/denoise-memory-and-suffix-switch`）

- **改了什么**：`gui/MainWindow.xaml` 两个 `Content` 逐字改为「指定宽度」「指定高度」，并更新行内实测注释；
  票面「不改」四项（`.cs` 注释、引擎英文 usage、spec 模式名、`setting.ini` 键）逐项未动。
  （行号：实施后为 `:68`/`:70`，票面表格写的是改前 `:64`/`:66`。）
- **两轴判定（r1）**：spec `approved` **0 发现** · standards `approved` **0 发现**（本批次唯一一次零发现）。
- **gate**：`.pi-implement\gate.cmd` → **PASS，91 passed**（编排者复跑，31s）。
- **红→绿实证（UIA，780×540）**：改前 radio 名 `指定宽`/`指定高`、行宽 457px → 改后逐字新文案、行宽 481px
  ≤ 可用 720px（余 239px）、`ROWS=1` 单行不换行；gate 重建后复测数字逐字相同。
- **行为不变（端到端）**：指定宽度 → `in-(waifu2x_cunet)-n0-200x.png` 200×200 +
  `LastScaleMode=width/LastScaleWidth=200`；指定高度 → 同理。
- **实测修正了票面估算**：票面预估 +48px，实测 +24px（每标签 +12px）；两种情况均不换行。
- **残余风险**：行宽为本机字体实测值；字号/主题变化可能带来像素微差（481 ≪ 720，结论不依赖该余量）。
- **提交**：`e6fd3ff`；**未 push**（AGENTS.md：发布动作先经维护者确认）。

### 人工验收（2026-09-20 维护者定案：默认通过）

- **未逐条执行的项**：尺寸模式文案视觉。
- **口径**：维护者无空操作逐条跑，按 `AGENTS.md`「人工验收默认通过（固定流程）」视为通过并结清；AC 复选框保持未勾，如实反映「没人跑过」。
- **重开方式**：日后维护者提及其中任一项 → 重开本票或另开新票。
- 参考：`docs/lessons.md` §3.16。
