# 53: 尺寸模式文案「指定宽 / 指定高」→「指定宽度 / 指定高度」

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** ready-for-agent

> *This was generated by AI during triage.*

## 报告（维护者，2026-09-20，implement-pro 批次进行中）

尺寸模式那行的两个单选按钮文案要写全：「指定宽」→「指定宽度」，「指定高」→「指定高度」。

## 方案要点

- 只改两处 XAML 文案（`gui/MainWindow.xaml`）：

  | 位置 | 现在 | 改为 |
  |---|---|---|
  | `:64` | `Content="指定宽"` | `Content="指定宽度"` |
  | `:66` | `Content="指定高"` | `Content="指定高度"` |

- **不改**：`gui/MainWindow.xaml.cs:514` 的代码注释（`// 指定宽值`）、引擎 `--width/--height` 的英文
  usage、spec 里的模式名（`ratio | width | height`）、`setting.ini` 键（`LastScaleWidth/Height`）。
- 该行是 `WrapPanel`（工单 19：窄窗口自动换行），文案变长只影响换行点，不影响 `MinWidth`。

## 变更文件

- `gui/MainWindow.xaml`（两处 `Content`）。

## Acceptance criteria

- [ ] 两个单选按钮逐字为「指定宽度」「指定高度」
- [ ] 尺寸模式的功能行为不变：选中后仍传 `--width` / `--height`，`setting.ini` 键值不变
- [ ] 窗口最小尺寸（780×540）下不出现裁剪（该行 `WrapPanel` 允许换行，`MinWidth` 不变）
- [ ] 运行态实测记录该行在 780 宽下的实际占用与是否换行（写入证据）

## 未决 / 风险

- 文案 +2 字 ×2（约 +48px）：780×540 最窄宽度下该行可能由一行变为两行（`WrapPanel` 设计如此）。
  若维护者不接受换行，需另定（缩短文案/收窄两个输入框）——默认按允许换行实施。
- 「降噪与输出」行（工单 51）行宽余量仅 4px，本票不动该行；若后续再往该行加内容需重新实测。
