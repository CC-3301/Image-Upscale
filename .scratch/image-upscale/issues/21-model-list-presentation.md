# 21: 模型框文案与排序（"现有模型"、A-Z、去掉类型/架构灰字）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（随 v0.2.1 发布：https://github.com/CC-3301/Image-Upscale/releases/tag/v0.2.1）

> *This was generated by AI during triage.*

## 报告（2026-09-14 人工测试）

模型框不要写"打包模型"，写"现有模型"；按 A-Z 排序；后面不要出现类型和架构信息。

## Triage 验证记录（代码确认）

`gui/MainWindow.xaml`：标签 `TextBlock Text="打包模型："`；`MainWindow.xaml.cs OnModelChanged` 在框右侧显示灰字 `ModelGroupText.Text = $"类型：{m.Group}　架构：{m.Arch}"`；模型列表按 `manifest.conf` 顺序装入，未排序。

## 分诊建议

推荐 `ready-for-agent`：

- 标签改"现有模型："（"打包"是实现视角词汇，CONTEXT.md 的"模型"定义即用户视角）
- 显示名按 `StringComparer.OrdinalIgnoreCase` 排序后装入（manifest 文件顺序不动，GUI 展示层排序即可）
- 移除 `ModelGroupText` 显示

注意：spec 实现决策写"GUI：模型选择（分组显示）"——本工单落地后与该句冲突，需同步把 spec 措辞更新为"列表展示（A-Z）"；类型/架构信息保留在 manifest 供引擎用。

## Comments

### Agent Brief（2026-09-14 推进 ready-for-agent）

**Category:** enhancement
**Summary:** 模型选择框改为"现有模型"文案、A-Z 排序、移除类型/架构展示

**Current behavior:**
模型标签为"打包模型："（实现视角词汇），框右侧显示"类型：xx 架构：xx"灰字，列表按清单文件顺序排列。

**Desired behavior:**
标签为"现有模型："；显示名按字母序（不区分大小写）排列；不再显示类型与架构（信息保留在模型清单供引擎与排障使用，仅不在主界面展示）。

**Key interfaces:**
- 模型清单解析与加载顺序不变，仅 GUI 展示层排序
- 产品规格中"模型选择（分组显示）"的措辞需同步更新为本工单口径

**Acceptance criteria:**
- [ ] 标签显示"现有模型："
- [ ] 列表按显示名 A-Z 排序（不区分大小写）
- [ ] 类型/架构文字不再出现在主界面
- [ ] spec 的"分组显示"措辞已同步更新

**Out of scope:**
- 模型清单文件格式变更
- 模型分组下拉/子菜单（用户明确不需要）

### 实施记录（2026-09-14）
- "现有模型："标签；显示名 A-Z 排序（OrdinalIgnoreCase）；移除类型/架构灰字
- 人工：列表视觉核对
