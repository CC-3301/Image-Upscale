# 66: `MainWindow` 里 `AddSuffix*` 与 `Suffix*` 两套名字族并存（工单 60 第 2 轮的收窄决定）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（命名族已统一，见 Comments）

> *This was generated by AI during triage.*

## 缘起

工单 60 第 2 轮命名收口（2026-09-20，维护者点头）只改了状态字段与两个转换 helper：
`_addSuffix` → `_suffixEnabled`、`AddSuffixFromIndex` → `SuffixEnabledFromIndex`、`AddSuffixToIndex` → `SuffixIndexFrom`（含形参）。
`AddSuffix*` 族的其余名字按定案**保留**——于是同一文件里现在并存两套词汇（r2 / r3 两轮 standards 轴都报了同一条）。

## 现状（`gui/MainWindow.xaml.cs`）

新族（状态式）：`_suffixEnabled`（:89）、`SuffixEnabledFromIndex`（:657）、`SuffixIndexFrom`（:660）。
旧族（动作式，保留）：
- `AddSuffix` 字段（`SettingsStore`，:626）与 `DefaultAddSuffix`（:652）
- `AddSuffixLabels`（:648，界面项表）
- `UpdateAddSuffixState`（:360）、`OnAddSuffixChanged`（:370）
- `_suppressAddSuffixWrite`（:91）
- `gui/MainWindow.xaml` 的 `AddSuffixBox`（:123）
- ini 键 `LastAddSuffix` —— **不能改**（改了会丢用户设置）

后果：读者在 `_suffixEnabled` 与 `_suppressAddSuffixWrite` 相邻两行内要切两次词汇；`CONTEXT.md` 的术语表只认「文件添加扩展名」，两套族名让 `rg` 检索同名概念时要试两次。

## 方案要点

- 选定一族做统一：**实际定案是统一到 `AddSuffix*`**（含把工单 60 r2 刚改出的 `_suffixEnabled` 也收进族里成 `_addSuffixEnabled`）——ini 键 `LastAddSuffix` 与 `CONTEXT.md` 的「文件添加扩展名」都在这条词根上，这样 `gui/` 内才真正只剩一族（留 `_suffixEnabled` 反而破族）。
- **`LastAddSuffix` 无论如何不改**。
- 涉及 XAML 控件名（`AddSuffixBox`）时注意与 `x:Name` 的引用点同步（`UpdateAddSuffixState` / `OnAddSuffixChanged` 的挂接）。
- 属纯改名，零行为变化。

## Acceptance criteria

- [x] 该概念在 `gui/` 内只有一族词汇（`rg` 可证，全为 `AddSuffix*` 词根），ini 键 `LastAddSuffix` 保持不变
- [x] 行为零变化：默认开、文件夹输入灰置显示「开」、models 缺失时不改写记忆值
- [x] `dotnet build gui/ImageUpscaleGui.csproj -c Release` 0 警告 0 错误，`python -m pytest tests -q` 全绿（100 passed）

## 未决 / 风险

- 纯品味 + 检索性收益，无用户可见影响；维护者 2026-09-20 的定案是「本轮只改状态字段与 helper」，本票是那次收窄留下的尾巴。
- 按 `AGENTS.md`「同文件同区域攒批」处理：将来正好要动 `MainWindow.xaml.cs` 的设置层时一次闭环，不单跑轮。

## Comments

### 批次收口（2026-09-20，批次 64+65+66 一次闭环）

- **交付（1 个 commit）**：`b051c0d` —— `_suffixEnabled` → `_addSuffixEnabled`、`SuffixEnabledFromIndex` → `AddSuffixEnabledFromIndex`、`SuffixIndexFrom` → `AddSuffixIndexFrom`（含形参与三处注释）。
- **定案方向**：统一到 `AddSuffix*`（而非票面括号里提的「只保留 `_suffixEnabled`」）——只有这样才能满足 AC1「`gui/` 内只有一族词汇」；`AddSuffix` 词根同时对齐 ini 键 `LastAddSuffix` 与 `CONTEXT.md` 的「文件添加扩展名」。票面「方案要点」已按此订正。
- **保留不动**：ini 键 `LastAddSuffix`（改了会丢用户设置）、`AddSuffixLabels`、`DefaultAddSuffix`、`s.AddSuffix`、`AddSuffixBox`、`UpdateAddSuffixState`、`OnAddSuffixChanged`、`_suppressAddSuffixWrite`、界面文案；`gui/MainWindow.xaml` 未动。
- **行为零变化的依据**：6 个读写点 1:1 改名、表达式未变；`dotnet build gui` 0 警告 0 错误（XAML 引用一致）+ `pytest tests -q` **100 passed**。
- **两轴轨迹**：r1 双 approved（1 P2 为票面口径差）→ 无修复轮（纯改名一次过）→ r2 / r3 / r5 / r6 复审均无新项。
- **残余风险**：无。

### 后续变更（工单 68，2026-09-20）

- 工单 68 带来第二个开/关开关（删除输入文件），实施时把开关标签表与两个转换 helper 统一为 `Switch*`（`SwitchLabels` / `SwitchEnabledFromIndex(idx, fallback)` / `SwitchIndexFrom`），`AddSuffixLabels` / `AddSuffixEnabledFromIndex` / `AddSuffixIndexFrom` 随之消失。
- 本票 AC1「`gui/` 内只有一族词汇（全为 `AddSuffix*` 词根）」自此**不再成立**，其实质目标改由工单 68 接管：**同一概念只有一处表与一族名字**（`rg SwitchLabels` 可证）。ini 键 `LastAddSuffix`、`DefaultAddSuffix`、`AddSuffixBox`、`s.AddSuffix` 仍不动。
