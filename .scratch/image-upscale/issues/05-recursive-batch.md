# 05: 递归批处理

**Parent:** .scratch/image-upscale/spec.md

**What to build:** 用户把整个文件夹（含多层嵌套，如 A 含 B、B 含 C）交给引擎一次处理：所有匹配后缀的图片被处理，输出中只有顶层 A 被重命名，内部 B/C 目录与图片文件名原样镜像（与 waifu2x-caffe 一致）。单个文件失败不影响其余文件，最后给出汇总。

**Blocked by:** 01

**Status:** done（自动化验收全绿）

- [ ] 文件夹输入递归遍历，处理全部 .jpg/.jpeg/.png/.webp 文件
- [ ] 仅顶层重命名为 `A-(模型名)-[nN-]<参数段>`（模型名段括号包裹，降噪段按档位可选），内部子目录结构与文件名完全不变
- [ ] 单文件失败不中断批处理，失败文件计入汇总并反映在退出码
- [ ] 处理结束时输出成功/失败汇总（stderr 行式）
- [ ] 与尺寸模式、降噪档位、输出格式/质量参数正确组合
