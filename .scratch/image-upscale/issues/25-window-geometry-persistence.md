# 25: 记住上次关闭时的窗口大小和位置

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（随 v0.2.2 发布：https://github.com/CC-3301/Image-Upscale/releases/tag/v0.2.2）

> *This was generated by AI during triage.*

## 报告（维护者 2026-09-15 迭代需求 1）

新的记忆设置：记住上次关闭时的窗口大小和位置。

## 方案要点

- 扩展 `setting.ini`（工单 14 机制）：`LastWindowLeft/Top/Width/Height/Maximized`（`Last*` 键名惯例一致）
- 退出时写入：正常态写当前 Left/Top/Width/Height；最大化态写 `RestoreBounds`（最大化坐标不是恢复坐标）+ Maximized=1
- 启动时恢复：先设尺寸，再设位置；最大化则恢复尺寸后置 `WindowState=Maximized`
- 边界校验：与虚拟屏幕（`SystemParameters.VirtualScreen*`）无交集时放弃该值（显示器拔掉/分辨率变更防丢窗口），位置回退居中
- 与工单 14 的静默回退语义一致：值非法/缺失逐项回退，不崩溃

## Acceptance criteria

- [ ] 拖动并调整窗口 → 关闭 → 重启：位置与大小恢复
- [ ] 最大化状态关闭 → 重启：以最大化打开；还原后回到关闭前的正常位置尺寸
- [ ] ini 中写入屏外坐标（如 -99999）→ 启动无异常，窗口出现在可见区域
- [ ] 删除 ini → 默认尺寸位置启动

## Comments

### 实施记录（2026-09-15）
- setting.ini 新增 LastWindowLeft/Top/Width/Height/Maximized；退出写 RestoreBounds（最大化/最小化态）；启动恢复（尺寸→位置→最大化），位置与虚拟屏幕无交集或超出屏幕则回退默认；OnClosing 重构为逐项覆盖，引擎缺失场景旧设置不丢。人工：拖动/调整→重启恢复；最大化关闭→重启还原；手写屏外坐标→窗口可见。
