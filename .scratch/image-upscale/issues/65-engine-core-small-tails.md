# 65: `engine_core.cpp` 的两条小尾巴（默认滤镜字面量 / 聚合初始化错位兜底）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（两条均收口，见 Comments）

> *This was generated by AI during triage.*

## 缘起

批次 59-63 的 r3 评审记录项。按 `AGENTS.md`「小问题不单开票、不单跑轮」的处置，攒批登记，
按**同一区域（`engine/src/engine_core.cpp`）**归成一批。两条都不影响对外行为。

## 两条内容

1. **`RunOptions::down_filter = 0;` 用字面量重述了同文件的常量**：`RF_LANCZOS3`（约 `:474`，`:1116` 已用它作 CLI 默认）。
   同一个「默认重采样滤镜」概念在文件里有两种写法；同批次的 GUI 半边（工单 60）刚把同形字面量收成了
   `SettingsStore.DefaultDownFilterIndex`，对齐 `lessons §1.4`「一个概念只允许一处真相」。
   最小修法：初值写 `RF_LANCZOS3`（枚举隐式转 `int`，无新警告风险）。
2. **聚合初始化的错位风险只消除了一半**：工单 61 把三处逐字重复的实参表收成一处 `run_arch`，
   工单 61 的 r3 又给 `NamingSegments` / `RunOptions` 补了类内默认初值——但那只挡住「漏写花括号 → 未定义值」；
   全字段花括号初始化时**换序/中途插字段仍会静默错位**（相邻多个 `int` / `bool`，编译器不报错）。
   `CMakeLists.txt` 是 `CMAKE_CXX_STANDARD 17`，指定初始化器不可用。可选方向：
   升到 C++20 用指定初始化器、改成具名赋值、或用一个构造函数集中绑参。

## Acceptance criteria

- [x] `RunOptions::down_filter` 的默认值与 CLI 默认来自同一处定义（`RF_LANCZOS3` 定义在 `engine_core.cpp:474`，`:647` 与 CLI 默认 `:1117` 同源，无第二处字面量）
- [x] `NamingSegments` / `RunOptions` 的构造点不再存在「换序/插字段静默错位」的可能（两个构造点均改逐字段具名赋值；「新增字段漏赋值」的残余风险已在注释里写明）
- [x] 引擎对外行为零变化（产物命名全表、退出码、行协议）
- [x] `scripts\engine-build.bat` 警告数不高于基线（`cl D9025` + `engine_core.cpp` C4244，逐条与基线相同），`python -m pytest tests -q` 全绿（100 passed）

## 未决 / 风险

- 第 1 条是一行改动；第 2 条可能牵动构建标准（C++20）或结构体形态，属判断项，收益是维护期安全，用户不可见。
- 单独为它开一轮不划算；建议排在「正好要动引擎主干」的版本，与 `run_files` 附近的其他重构一起做。

## Comments

### 批次收口（2026-09-20，批次 64+65+66 一次闭环）

- **交付（3 个 commit）**：`98f275a`（`down_filter = RF_LANCZOS3` + 两个构造点改逐字段具名赋值）→ `c490df4`（具名赋值改 IIFE 恢复 `const`、术语统一为「指定初始化器」）→ `ae5ff4f` + `80dbf43`（注释口径：哨兵项与「与 CLI 同值」项分清、`format` 空串不算哨兵且落 WebP 分支）。
- **方案取舍**：**未升 C++20**（构建配置变更超本票，需维护者单独定）；用「IIFE 取返回值 + 体内逐字段具名赋值」同时拿到「不依赖字段顺序」与 `const`。换序/中途插字段不再静默错位。
- **残余风险（已在代码注释写明）**：新增字段漏赋值编译器仍不报，靠类内初值兜底；其中 `down_filter` / `tilesize` 的初值本身就是合法 CLI 默认值，漏填是静默取默认而非「未设置」。
- **越界发现（未做，另行记录）**：`-f` 的 `format` 空串在 CLI 侧无校验，会落进 WebP 分支而不报错——既有行为，非本票范围。
- **两轴轨迹**：r1 双 approved（3 P2）→ fix → r2 双 approved（3 P2）→ fix → r3 双 approved（1 P2）→ fix → r4（1 P2）→ fix → r5 / r6 收敛轮均无新项。
- **gate**：`scripts\engine-build.bat` 警告与基线逐条相同（`cl D9025` + `engine_core.cpp` C4244），`pytest tests -q` **100 passed**。
