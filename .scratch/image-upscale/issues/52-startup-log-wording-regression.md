# 52: 启动日志文案回归：`引擎就绪，已加载 N 个模型` → `就绪，已加载 N 个模型`

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（代码与构建完成；启动行文案按维护者 2026-09-20 定案默认通过，见 Comments）

> *This was generated by AI during triage.*

## 报告（维护者，v0.2.8 后）

启动后日志首行现在是 `引擎就绪，已加载 7 个模型`；维护者要求回到 `就绪，已加载 7 个模型`——
「这个之前说过了，又给改回去了」。

## 根源（代码 + 工单确认）

- `gui/MainWindow.xaml.cs:100`：`Log($"引擎就绪，已加载 {_models.Count} 个模型");`
- 工单 33 的 2026-09-19 补记写明：「启动行文案补齐为『**引擎**就绪，已加载 N 个模型』（原少
  「引擎」二字）」—— 这次「补齐」未经维护者要求，属于把维护者此前定过的文案改回去了。

## 方案要点

- 只改文案：`就绪，已加载 {_models.Count} 个模型`；错误分支（models 缺失 / 清单解析失败）不动。
- 工单 33 文件追加评论：记录 2026-09-19 那次「补齐」被回退，验收口径回到不带「引擎」二字。

## 变更文件

- `gui/MainWindow.xaml.cs:100`
- `.scratch/image-upscale/issues/33-startup-log-one-line.md`（补评论，改验收口径）

## Acceptance criteria

- [ ] 正常启动后日志区仅一条启动行，逐字为 `就绪，已加载 7 个模型`
- [ ] models 缺失 / `manifest.conf` 解析失败时仍显示原来的错误行

## 未决 / 风险

- 无。文案改动不涉及引擎与行协议。

## Comments

> *This was generated by AI during triage.*

### 实施记录（2026-09-20，implement-pro 批次 `feat/denoise-memory-and-suffix-switch`）

- **改了什么**：`gui/MainWindow.xaml.cs:100-101` 启动行文案 `引擎就绪，已加载 N 个模型` →
  `就绪，已加载 N 个模型`（错误分支两条未动）；`33-startup-log-one-line.md` 标题与验收口径同步，
  并作废 2026-09-19 那次未获维护者要求的「补齐」。
- **两轴判定（r2）**：spec `approved`（1 条 P2 记录项：缺一次带界面人工启动记录）；standards `approved`（0 发现）。
- **r1 裁决留痕**：实施者自加的 `tests/test_startup_log_wording.py`（对 GUI 源码做字面量断言）
  两轴均判 P1 越界（与 spec「GUI 无自动化测试」「只断言外部可见行为」+ lessons §2.5/§3.5 冲突）
  → 编排者裁决**删除**（采纳 spec 报告的最小修法 1：回归手动验收 + 票面记录）；保留它须先改
  `spec.md` 的测试缝口径，为 1 行文案改测试口径不划算。
- **gate**：`.pi-implement\gate.cmd`（引擎构建 + GUI 构建 + `python -m pytest tests -q`）→
  **PASS，91 passed**（与基线同数，符合「删掉越界测试」的预期）；编排者复跑结论一致。
- **残余风险**：启动行文案现无自动化守卫（按上述裁决接受）；「启动后仅一条启动行」需人工
  带界面核验（本票人工验收项）。
- **提交**：`97bd96d`（初版，含已删测试）→ `cad32c4`（r2 收口）；**未 push**
  （AGENTS.md：push/发布动作须先经维护者确认）。

### 人工验收（2026-09-20 维护者定案：默认通过）

- **未逐条执行的项**：启动行文案启动核验。
- **口径**：维护者无空操作逐条跑，按 `AGENTS.md`「人工验收默认通过（固定流程）」视为通过并结清；AC 复选框保持未勾，如实反映「没人跑过」。
- **重开方式**：日后维护者提及其中任一项 → 重开本票或另开新票。
- 参考：`docs/lessons.md` §3.16。
