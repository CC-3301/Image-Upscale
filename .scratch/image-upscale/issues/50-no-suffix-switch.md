# 50: 产物命名开关「添加扩展名」（引擎 `--no-rename`，只作用于文件输入）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（代码与构建完成；引擎 CLI 守卫已自动化验收，「关」的 GUI 效果按维护者 2026-09-20 定案默认通过，见 Comments）

> *This was generated by AI during triage.*

## 报告（维护者，v0.2.8 后）

加一个开关控制产物是否带命名后缀（`-(模型名)-nN-<倍率|尺寸>`），让产物直接用原文件名。

## 编排者交接（2026-09-20，implement-pro 批次）

以下三项是同批其他工单的 out-of-scope 转交，**归本票负责**（都在本票的「命名语义」范围内）：

1. **`README.md:84` 语义复查**：工单 51 已写入「关：控制文件输入的产物名是否带后缀段」的描述，
   但本票未落地时该描述是提前的。本票落地后必须逐字复查该句与实际行为（含「文件夹输入不生效，
   下拉固定为开并灰置」后半句）一致。
2. **`CONTEXT.md` 术语**：新增术语「文件添加扩展名」（= 是否给产物名加 `-(模型名)-nN-<倍率|尺寸>` 段），
   并明确与「文件格式扩展名」（`.jpg/.png`）不同义；保持最小增补，不重写现有词条。
3. **`docs/lessons.md` 不改**：§2.4 持久化清单的补漏（含 `LastAddSuffix`）归工单 49，本票不动。

另：界面控件已由工单 51 落位（`AddSuffixBox` + `LastAddSuffix`），本票**只加引擎开关与传参**，
不做布局改动（「降噪与输出」行实测余量仅 4px，任何新增控件都需重测）。

## 维护者定案（2026-09-20 本轮 triage）

1. **开关只作用于文件输入**：目标是文件夹（递归批处理）时该开关**不生效**，永远按现有规则命名
   —— 输出到 `A-(模型名)-nN-<倍率|尺寸>/` 目录、内部结构原样镜像。
   （理由：目录模式去掉输出目录段会把产物写回源目录，二次运行会把产物当输入再放大一轮。）
2. 关闭时（文件输入）产物 = 源文件同目录 + **原文件名** + 输出格式扩展名；直通缩放的
   `-(Resize)-<尺寸>` 段同样不加。例：`B/A.png` + `-f jpg` → `B/A.jpg`。
3. **已存在文件的处理不变**：仍直接覆盖（spec 用户故事 18）。**不做**「跳过并记日志」，
   不引入 `--on-exists` 参数（原提案的跳过策略由维护者否决）。
4. **安全守卫**：关闭后缀且输出格式与输入相同（`A.png` + `-f png`）时目标路径就等于输入路径，
   必须**拒绝写盘**并报错，绝不覆盖源图。退出码走 IO 失败（3）。

## 方案要点

- 引擎新增布尔开关 `--no-rename`（`engine/src/engine_core.cpp`）：`print_usage` 文案 + 参数解析 +
  `single_file_outpath` 增一条分支（`in.parent_path() / (in.stem() + "." + ext)`）+ 目录模式忽略该开关。
- 守卫：生成 `outpath` 后比较 `weakly_canonical(outpath) == weakly_canonical(inpath)` → 命中即
  `fail(false, "refuse to overwrite input", outpath)` 并跳过该文件（不推理、不写盘）→ 退出码 3。
- GUI：新增「文件添加扩展名」下拉（`开` / `关`，位置与控件形态见工单 51），setting.ini 新增
  `LastAddSuffix`（默认 1 = 开，与现状一致）；仅当「输入是文件」且选择为「关」时传 `--no-rename`。
- GUI 日志映射：`refuse to overwrite input: ` 开头的 stderr 行 → 中文提示
  「✘ 拒绝覆盖输入文件（产物与输入同名，请把「文件添加扩展名」设为开）：<文件名>」。

## 变更文件

- `engine/src/engine_core.cpp`：`print_usage`、参数解析、`single_file_outpath`、自毁守卫。
- `gui/MainWindow.xaml.cs`：`LastAddSuffix` 读写（#51 落位）、发参、日志映射。
- `tests/test_naming.py`：新增 6 条守卫（含直通缩放、目录忽略、拒写自毁）。
- `tests/conftest.py` + `tests/test_auto.py`：`make_gradient` / `resolved_levels` 助手搬到 conftest 共用
  （**评审后补进清单** —— 原清单未列这两个文件，spec 轴 r3 报为「越界，report-only」；行为等价、无功能影响）。
- `.scratch/image-upscale/spec.md`（命名规则段、GUI 段、冲突策略段）、`README.md`、`CONTEXT.md`（新术语词条）。

## Acceptance criteria

- [x] 不传 `--no-rename`：命名全表与现状逐字一致（现有 `test_naming.py` 全绿即证）
- [x] `B/A.png` + `-f jpg --no-rename` → 产物 `B/A.jpg`（无任何后缀段）
- [x] 直通缩放 + `--no-rename` → `B/A.jpg`（不出现 `-(Resize)-` 段）
- [x] `B/A.png` + `-f png --no-rename` → 退出码 3、stderr 含 `refuse to overwrite input`、`B/A.png` 未被改写
- [x] 目录输入 + `--no-rename` → 仍输出到 `B-(模型名)-n0-2.0x/` 目录、内部名原样镜像（开关被忽略）
- [x] 目录输入 + `--no-rename` + 关掉 AUTO 一致性时，内部 `-nN` 段行为不变（工单 39 语义不回归）

## 未决 / 风险

- 「扩展名」是维护者的用词，实现口径 = 是否给产物名加 `-(模型名)-nN-<倍率|尺寸>` 这段后缀
  （**不是**文件格式扩展名 `.jpg/.png`）。若维护者本意不同，本条先改再动代码。
- UI 定案（2026-09-20 维护者）：标签为「文件添加扩展名」，控件为下拉（`开` / `关`）；
  输入为文件夹时下拉显示「开」并灰置（工单 51）。

## Comments

> *This was generated by AI during triage.*

### 实施记录（2026-09-20，implement-pro 批次 `feat/denoise-memory-and-suffix-switch`）

- **改了什么**：引擎 `--no-rename`（**仅单文件输入生效**，目录批处理忽略）+ 「产物与输入同路径」拒写守卫
  （不推理、不写盘 → `io_failures` → **退出码 3**）+ GUI 发参与中文日志映射 + `tests/test_naming.py`
  6 条 CLI 守卫；spec / README / CONTEXT 同步（含 #51 转交的三项）。
- **两轴轨迹**：r1 两轴 approved（spec 2 + standards 5 条 P2）→ fix r2 → **r2 两轴 needs-fixes**
  → fix r3 → **r3 两轴 approved**（剩 4 条 P2 全 report-only）。
- **r1 关键裁决**：两轴在 `is_same_path_ci` 的 `towlower` 上给出相反风险 → 采纳 standards 的修法
  （改 locale 无关的 `CompareStringOrdinal`）；spec 建议「删掉大小写折叠」被 reject（NTFS 默认不敏感，
  `A.PNG` 与 `A.png` 是同一文件，**拒写才是对的**）。
- **诚实留痕（重要）**：要求的西里尔字母「红→绿」用例**改前就是绿的**——`weakly_canonical` 把同一文件
  解析回盘上真实大小写，两条比较串逐字符相同（探针：MSVC 14.44 / locale 936，4 种拼写均归一为 `А.PNG`）；
  即 standards r1 P2-4 描述的「非 ASCII 折叠漏检→覆盖源图」**在本代码路径不可达**。处置：修法保留
  （去 locale 依赖本身正确）、用例降级为**回归守卫**并在 docstring 如实声明，不冒充红→绿。
- **P1 红→绿（r3 实证）**：r2 搬测试助手时误删 `tests/test_auto.py` 的 `@needs_engine` → 引擎未构建时
  该用例由 skip 变 error；修复后实测：改名 `bld/image-upscale.exe` → `1 skipped` +
  「Skipped: engine exe not built」（编排者独立复跑确认）。
- **gate**：`.pi-implement\gate.cmd` → **PASS，98 passed**（基线 91 + 7 新守卫），引擎构建 + GUI 0 警告。
- **残余风险（均为已知、已裁定）**：`weakly_canonical` 失败时改为回退比较原始路径串（实测不可达，
  但已从「放行」改为「不放行」）；目录级 NTFS 大小写敏感目录下会误拒；目录模式忽略开关且无提示；
  同目录同名但不同源文件照旧覆盖（覆盖语义未变，spec 用户故事 18）。
- **新候选票（批次末尾汇总）**：56 = `run_files` 20 个位置参数 / 3 处重复调用表（内部 API 重构）；
  57 = 「是否文件输入」谓词在 `MainWindow.xaml.cs` 三处重复。
- **提交**：`8655709` → `969acfd`（r2）→ `dae1da7`（r3）；**未 push**（AGENTS.md：发布动作先经维护者确认）。

### 定案变更记录（2026-09-20，发布前）

> *This was generated by AI during triage.*

维护者定案：**定案 4 的拒写守卫撤销** ——「关 + 输出格式与输入相同时目标即输入路径」改为
**直接覆盖源文件**（原地替换），不新增二次确认/界面提示/源图备份。理由：与 spec 用户故事 18
「同名直接覆盖」一致、不再有例外。实施见工单 58；本票其余定案（`--no-rename` 只作用于文件输入、
文件夹固定添加后缀）不变。

- 上文定案 4 原文保留作历史记录，不再作为现行验收标准；现行口径以 `spec.md` 命名规则段 /
  冲突策略段 + 工单 58 为准。

### 人工验收（2026-09-20 维护者定案：默认通过）

- **未逐条执行的项**：关「文件添加扩展名」时的 GUI 效果（引擎 CLI 守卫已自动化验收）。
- **口径**：维护者无空操作逐条跑，按 `AGENTS.md`「人工验收默认通过（固定流程）」视为通过并结清；AC 复选框保持未勾，如实反映「没人跑过」。
- **重开方式**：日后维护者提及其中任一项 → 重开本票或另开新票。
- 参考：`docs/lessons.md` §3.16。

### AC 复选框勾选（2026-09-20，按「人工验收默认通过」口径）

- 本票 AC 含两类：① 自动化已验收项（引擎 CLI 守卫、`test_naming.py` 全绿等，见本票 Comments 的实现/验证记录）；② **未执行**的人工项（GUI 视觉/交互/运行态实测）。
- 两类一并勾为完成，依据 = 维护者 2026-09-20 定案「人工验收无空操作即默认通过」（`AGENTS.md` 固定流程 / `docs/lessons.md` §3.16）。
- **口径**：**勾选不代表跑过**；未执行项即本票「人工验收（默认通过）」小节列出者。日后维护者提及任一项 → 重开本票或另开新票。
