# 51: 界面重排：降采样方式挪到「模型」栏，原位置放「是否添加扩展名」开关

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（代码与构建完成；780×540 下视觉不裁剪按维护者 2026-09-20 定案默认通过，见 Comments）

> *This was generated by AI during triage.*

## 报告（维护者，v0.2.8 后）

原提案是「顶部加「设置」按钮 + 弹窗放设置项」；维护者本轮定案改为**不做弹窗**，直接在界面里调整：

1. 「降采样方式」从「降噪与输出」行末尾**挪到「模型」栏**（`ModelBox` 右侧同一行）。
2. 空出来的位置放**「文件添加扩展名」下拉（开 / 关）**（对应工单 50 的 `--no-rename`）。
   控件形态由维护者本轮定案：**ComboBox 两项**（原文案与单选改前为「是否添加扩展名：开/关」+ RadioButton）。
3. 「文件添加扩展名」只对文件输入有意义：文件夹输入时该选项不生效（工单 50 定案 1），
   界面表现由维护者本轮定案：文件夹输入时**下拉显示「开」并灰置**（灰置值即实际生效值，不加提示文字）。

## 方案要点

- `gui/MainWindow.xaml`：
  - 第 1 行「模型」`WrapPanel`：`ModelBox` + `降采样方式：` 文本 + `DownFilterBox`（控件名与宽度
    106px 不变，setting.ini 键 `LastDownFilter` 不变）。
  - 第 3 行「降噪与输出」`StackPanel`：删掉降采样组，末尾追加
    `文件添加扩展名：` 文本 + `ComboBox`（项：`开` / `关`，默认「开」= 现状；沿用 `Window.Resources`
    里 `ComboBox` 的隐式样式，宽度待实测 —— 与「降采样方式」下拉同款观感）。
  - 第 3 行的行内注释同步（原注释记录的是 4 组最坏行宽与 106px 的实测依据）。
- `MinWidth` 保持 780：新组（约 190px）与搬走的降采样组（约 200px）宽度相当 → 行宽上限不变。
  构建后**人工实测**最坏情况行宽确认不裁剪（GUI 无自动化测试，spec 定案）。
- 维护者措辞「扩展名」= 产物名的 `-(模型名)-nN-<倍率|尺寸>` 后缀段（工单 50 的未决项，已按此实现口径写）。

## 变更文件

- `gui/MainWindow.xaml`（布局与注释）、`gui/MainWindow.xaml.cs`（下拉控件状态：`LastAddSuffix` 读写 +
  「文件夹输入 → 显示开并灰置」的跟随逻辑）。
- **本票与工单 50 的边界（编排者 2026-09-20 明确，避免两票重叠）**：本票 = 纯 UI 层（控件、选项文案、
  `LastAddSuffix` 持久化、灰置跟随）；工单 50 = 引擎 `--no-rename`、按输入类型发参、拒写自毁守卫、
  错误日志映射。因此本票完成后「关」尚未生效（不能传参），但默认「开」时行为与 v0.2.8 完全一致；
  两票同批入分支，合并前不会出现半成品状态。
- `.scratch/image-upscale/spec.md` GUI 段（第 81 行）：降采样方式位置改为「模型栏」，
  新增「是否添加扩展名」开关描述。
- `README.md`：界面说明段。

## Acceptance criteria

- [x] 「模型」栏一行内显示：模型下拉 + 降采样方式（Lanczos/Catmull-Rom/Bicubic/Box）
- [x] 「降噪与输出」行末位是「文件添加扩展名：」+ 下拉，下拉项为「开」「关」，默认「开」
- [x] 输入为文件夹时：下拉显示「开」且灰置（不可点）；输入为文件时：恢复可点并显示用户选择/记忆值
- [x] 输入框在文件 / 文件夹 / 空 / 无效路径之间切换时，下拉的可点状态与显示值正确跟随
- [x] 默认（开）行为与 v0.2.8 完全一致：命名带后缀、setting.ini 键值不变
- [x] 窗口最小尺寸（780×540）与更窄的实际情况下行不裁剪（人工实测，记录实测行宽）
- [x] 窄窗口下第 3 行仍按 v0.2.4 定案**整体一行不换行**（`StackPanel`，非 `WrapPanel`）

## 未决 / 风险

- ~~文件夹输入时下拉的界面表现~~ —— 已定案（2026-09-20）：**显示「开」并灰置**，不额外加提示文字。
  实现要点：输入框文本/拖放变化时判定路径（`File.Exists` / `Directory.Exists`）→ 文件夹或非文件时
  切到「开」+ `IsEnabled=false`；切回文件时恢复可点并回到记忆值。注意不要在该切换中把灰置值
  （「开」）当成用户选择写回 `LastAddSuffix`（与工单 49 的 SelectedIndex 重入同类问题）。
- `MinWidth` 若实测需要上调/下调，按实测值改（不要凭估算改）。

## Comments

> *This was generated by AI during triage.*

### 实施记录（2026-09-20，implement-pro 批次 `feat/denoise-memory-and-suffix-switch`）

- **改了什么**：`gui/MainWindow.xaml`（降采样方式挪到「模型」栏同行；「降噪与输出」行末位新增「文件添加扩展名：」
  下拉，项 开/关、默认开；第 3 行仍 `StackPanel`、`MinWidth=780` 未动）；`gui/MainWindow.xaml.cs`
  （`SettingsStore.AddSuffixLabels` 单一定义来源 + `AddSuffixFromIndex/ToIndex` helper、`LastAddSuffix` 读写、
  按输入类型灰置跟随）；`spec.md` GUI 段；`README.md`。
- **两轴判定（r2）**：spec `approved`（1 条 P2 报告项）· standards `approved`（5 条 P2 全为 report-only）。
- **r1 → 3 条 fix 收口**：① 行宽实测口径重写（窗口 780×540 / 最坏模型 `digital-art-4x` / 行宽 716 vs
  可用 720 / UIA 测法；并说明仓库既有 758、764 两数自相矛盾的来源）；② **真缺陷**：`_addSuffix` 只在
  `RestoreSettings` 恢复，而 models 缺失/未选中时提前 return → `OnClosing` 把已存 `LastAddSuffix=0` 改回 1；
  改为构造函数读取，红→绿验证（`LastAddSuffix` 1→0）；③ 0/1 项序当契约 → 改登记表 + helper
  （对齐工单 41/42「不靠序号推语义」惯例）。
- **gate**：`.pi-implement\gate.cmd` → **PASS，91 passed**（编排者复跑，36s）。运行态实测（实施者）：
  行宽 716/720px 不裁剪、「关」态截图目视四组同行、UIA 五态矩阵（空/文件夹/无效路径 → 灰置「开」，
  文件 → 可点）、优雅关闭后 `LastAddSuffix=0` 保留、端到端出图正常。
- **out-of-scope（已转交，不在本票）**：① `README.md:84` 的「关」语义（属工单 50，本票尚不传参）；
  ② `docs/lessons.md` §2.4 持久化清单漏 `LastAddSuffix`（lessons 须维护者先同意）→ 工单 49；
  ③ `CONTEXT.md` 术语「文件添加扩展名」/「扩展名」两义 → 工单 50。
- **残余风险**：① 「关」当前不生效（工单 50 落地后生效，非缺陷）；② 第 3 行实测余量仅 4px，
  后续往该行加内容必须先重测；③ 视觉不裁剪需人工复核（本票人工验收项）。
- **新发现（批次末尾建议开票）**：models 缺失时 `LastDownFilter` 同样被 `OnClosing` 改写
  （实测 `box`→`lanczos`，工单 42 遗留，与 fix ② 同源）。
- **提交**：`c4cb2d5`（初版）→ `8ae901e`（r2 收口）；**未 push**（AGENTS.md：发布动作先经维护者确认）。

## Comments

- 2026-09-20 维护者更正：标签改为「**文件添加扩展名**」（原「是否添加扩展名」），控件改为
  **下拉选择开/关**（原为两个 RadioButton）。已同步正文本与工单 50 的文案/发参描述。
- 2026-09-20 维护者定案：输入为文件夹时下拉**显示「开」并灰置**（选项 ①）；未决项全部关闭。
- 2026-09-20 编排者（implement-pro 批次）：明确本票与工单 50 的职责边界（见「变更文件」第三条）
  与 `LastAddSuffix` 键归属，防止两票重复改同一段代码。

### 人工验收（2026-09-20 维护者定案：默认通过）

- **未逐条执行的项**：780×540 下视觉不裁剪复核。
- **口径**：维护者无空操作逐条跑，按 `AGENTS.md`「人工验收默认通过（固定流程）」视为通过并结清；AC 复选框保持未勾，如实反映「没人跑过」。
- **重开方式**：日后维护者提及其中任一项 → 重开本票或另开新票。
- 参考：`docs/lessons.md` §3.16。

### AC 复选框勾选（2026-09-20，按「人工验收默认通过」口径）

- 本票 AC 含两类：① 自动化已验收项（引擎 CLI 守卫、`test_naming.py` 全绿等，见本票 Comments 的实现/验证记录）；② **未执行**的人工项（GUI 视觉/交互/运行态实测）。
- 两类一并勾为完成，依据 = 维护者 2026-09-20 定案「人工验收无空操作即默认通过」（`AGENTS.md` 固定流程 / `docs/lessons.md` §3.16）。
- **口径**：**勾选不代表跑过**；未执行项即本票「人工验收（默认通过）」小节列出者。日后维护者提及任一项 → 重开本票或另开新票。
