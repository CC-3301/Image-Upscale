# 30: waifu2x 系模型加 waifu2x_ 前缀，upconv_7_anime 改名 upconv_7_art

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（随 v0.2.2 发布：https://github.com/CC-3301/Image-Upscale/releases/tag/v0.2.2）

> *This was generated by AI during triage.*

## 报告（维护者 2026-09-15 迭代需求 6）

waifu2x 系列模型应该加上 waifu2x_ 的前缀，并且 upconv_7_anime 改名 upconv_7_art。

## 维护者定案（Q2 = B）

**目录名 + manifest 的 id/display 一起改**（GUI 下拉列表与输出文件名同步新名）。代价已确认接受：旧 `setting.ini` 记的模型 id 失配一次，回落列表第一项。

## 改名对照（三个 waifu2x 架构模型；realcugan/realesr/digital-art 不动）

| 现目录 | 新目录 = 新 id = 新 display |
|---|---|
| `upconv_7_anime_style_art_rgb` | `waifu2x_upconv_7_art` |
| `cunet` | `waifu2x_cunet` |
| `upconv_7_photo` | `waifu2x_upconv_7_photo` |

## 涉及点

- `models/` 本地目录改名（目录不入库，权重文件名不动）
- `models/manifest.conf`：三个 `model` 块的 `model`/`display`/`dir` 字段
- `scripts/fetch-models.ps1`：Extract-Prefix 目标目录名
- 引擎默认模型 id（usage 文案 + `model_id` 默认值 `upconv7-anime` → `waifu2x_upconv_7_art`）
- `tests/conftest.py` 的 `IU_MODEL` 默认值；全测试库 grep 旧 id
- 权重文件名（`noise0_scale2.0x_model.param` 等）与 blob 名**一律不动**（`in`/`out`/`denoise` 映射不受目录改名影响）

## Acceptance criteria

- [x] fetch-models.ps1 重跑后目录名与新清单一致
- [x] GUI 模型下拉显示新名；选 waifu2x_upconv_7_art 处理输出 `A-(waifu2x_upconv_7_art)-2.0x.jpg`
- [x] pytest 全绿（含模型定位、命名、降噪映射）

## Comments

### 实施记录（2026-09-15）
- 三个模型目录已改名：`waifu2x_upconv_7_art` / `waifu2x_cunet` / `waifu2x_upconv_7_photo`（权重文件名与 blob 名未动）
- manifest.conf 三块的 model/display/dir 同步；`tileauto cunet` 为分档策略 token，与模型 id 无关，保持不变
- fetch-models.ps1 的 Extract-Prefix 目标目录同步（zip 内原始前缀不变，落盘名改名）
- 引擎默认 `-m` 与 usage 文案、conftest `IU_MODEL` 默认值、全部测试断言名同步
- 验证：引擎重建 + pytest 68 全绿（含全部打包模型冒烟、降噪命名、manifest 定位）

### AC 复选框批量勾选（2026-09-20）

- 本票交付已随状态行列出的版本发布；当时只写了状态行与 Comments 记录，`- [ ]` 未逐条勾选。本次按**状态行的发布记录 + 本票 Comments 的实现/验证记录**把 AC 逐条勾为完成。
- **口径**：这是**账面补齐**，不是重新执行验收；勾选依据是既有发布记录，本次未新增验证。若日后发现某项实际未达成 → 回退该勾并另开票。
