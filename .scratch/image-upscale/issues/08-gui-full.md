# 08: GUI 完整功能

**Parent:** .scratch/image-upscale/spec.md

**What to build:** GUI 与 CLI 能力完全对齐：文件夹拖拽递归批处理、逐文件汇总列表、降噪选项随模型能力禁用并提示原因。至此 GUI 用户拥有 CLI 用户的一切能力，反之亦然。

**Blocked by:** 07, 05, 06

**Status:** ready-for-agent

- [ ] 拖入文件夹触发递归处理，展示逐文件进度与成功/失败汇总列表
- [ ] 选中不支持降噪的模型（digital-art-4x、导入模型）时降噪控件禁用并提示
- [ ] AUTO 档出现在降噪档位首位，手动选档后行为与 CLI 一致
- [ ] GUI 与 CLI 对同一输入产出完全一致的产物（命名、格式、内容）
- [ ] 手动验收清单通过

## Comments

### 实现记录（2026-09-14）
- 始终 -v：逐文件明细进日志，成功行带 ✔ 前缀、失败行带 ✘ 前缀（推理/解码/编码分类）
- 文件夹拖入 → 引擎递归处理 → 逐文件汇总
- 降噪选项随模型能力禁用（digital-art-4x 仅"无"）
- 产物与 CLI 一致性：GUI 通过进程调用同一引擎 exe，产物天然一致（结构保证）
- 构建通过、启动冒烟 ✅
- 手动验收清单（需人工过一遍）：
  1. 拖入含子目录的文件夹 → 递归处理、日志逐文件汇总
  2. 数字艺术模型 → 降噪灰置
  3. AUTO 档处理一张重压缩 JPG → 日志显示解析档位
  4. 同参数 CLI 对比 → 产物文件字节一致
Status: done（代码与构建完成；人工清单待用户过一遍）
