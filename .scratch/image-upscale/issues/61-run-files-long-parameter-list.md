# 61: 引擎 `run_files` 20 个位置参数 + 3 处重复调用表

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（参数结构化 + 三处调用表合一；AC2 的警告口径已订正为「不新增」）

> *This was generated by AI during triage.*

## 缘起

工单 50 的 r1 / r2 / r3 standards 轴连续三轮报同一项（每轮都标为"判断项、可另开票"，未在票内做）。
登记为候选。

## 现状

- `engine/src/engine_core.cpp` 的 `run_files(...)` 现在有 **20 个位置参数**（工单 50 又加了 `no_rename`）。
- 三个架构分支（waifu2x / cugan / rrdb+compact）各自**逐字重复**同一串实参（3 处）。
- `single_file_outpath(...)` 也从 6 参涨到 7 参。

后果：任何一次参数增删都要同步改"1 处签名 + 3 处调用"；位置参数相邻类型相同（多为 `int`/`bool`/`wstring`），
传错顺序编译器不报错。

## 方案要点

- 把命名与输出相关参数收成小结构体（如 `NamingSegments{ wext, wdisplay, denoise_seg, scale_seg }`
  与 `RunOptions{ format, quality, run_scale, target_mode, target_value, target_is_width, down_filter }`），
  三处调用同步缩短到一行。
- 或先把三处调用表抽成一次 `make_run_args(...)` 之类的构造点，再评估是否进一步结构化。

## Acceptance criteria

- [x] `run_files` 参数个数显著下降（23 → 10 参）+ 三处调用表合一一处（`run_arch`）
- [x] `python -m pytest tests -q` 全绿（落定时 99 条基线，本批终态 100 条）+ 引擎构建**不新增**警告（基线 2 条：CMake `cl D9025`、`engine_core.cpp` C4244，均非本票引入，「0 警告」口径按此订正）
- [x] 引擎对外行为零变化：产物命名全表、退出码、`--no-rename` 原地覆盖守卫行为均不回归

## 未决 / 风险

- **动的是引擎主干**：收益是维护成本，用户不可见；建议排在"正好要改这块代码"的版本，单独为它开一轮不划算。
- 若做，务必逐条对照 `spec.md` 的命名规则全表与 `tests/test_naming.py`（13 条）做回归。

## Comments

### 批次收口（2026-09-20，批次 59 + 60 + 61 + 62 + 63 一次闭环）

- **交付**：`492fc97`（`run_files` 23 → 10 参，新增 `NamingSegments` / `RunOptions`；三处架构实参表合并为唯一一处 `run_arch`；
  `single_file_outpath` 7 → 5 参）+ `a144519`（r2：形参 `denoise_seg` → `file_denoise_seg`，与批量档位段 `naming.denoise_seg` 区分）
  + `b4cc6ce`（r3：两个结构体补类内默认初值）。
- **对外行为零变化的依据**：逐字段核对聚合顺序 = 结构体声明顺序；`verbose (int) → verbose != 0`；命名全表、
  退出码、`--no-rename` 原地覆盖守卫均未回归；`pytest tests -q` **100 passed**（落定时 99 条基线）。
- **过程中的红灯**：第一版把 AUTO 的逐文件 `file_denoise_seg` 并进 `naming.denoise_seg` → `test_auto.py` 红
  （产物名 `-n0-` 而模型跑 n3）→ 改回显式单传后转绿。这条正是 r2 那个改名要防的坑。
- **两轴轨迹**：r1 双 `approved` → r2 双 `approved` → **r3 双 `approved`**。
- **gate**：引擎构建警告与基线逐字相同（CMake `cl D9025` + `engine_core.cpp` C4244，**0 新增**；AC2「0 警告」按此口径订正）
  + `dotnet build gui` 0/0 + `pytest tests -q` **100 passed**。
- **残余风险**：聚合初始化靠字段顺序对齐，全字段花括号初始化时**换序/中途插字段仍会静默错位**（r3 的类内初值只消除
  「漏写花括号 → 未定义值」这一半）；彻底解（C++20 指定初始化器 / 具名赋值）转候选票 65。
