# 62: GUI「是否文件输入」谓词三处重复

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（输入谓词已收敛到 `IsFileInput` / `IsValidInput`；AC1 字面口径已订正）

> *This was generated by AI during triage.*

## 缘起

工单 50 的 r3 standards 轴报出（当时判为批次候选，未在票内做）。

## 现状

`gui/MainWindow.xaml.cs` 里「这次输入是不是一个文件」这条判断写了三遍：

- 发参前判断是否追加 `--no-rename`（`File.Exists(input) && !_addSuffix`）
- `UpdateAddSuffixState` 的灰置跟随（决定下拉是否可点、是否强制显示「开」）
- 同一方法内的输入合法性前置校验（`!File.Exists(input) && !Directory.Exists(input)` → 参数错误）

三处语义一致但各自成文：以后输入面变化（例如支持新的输入形态、或把"文件夹"判定改严），
必须同步改三处，漏一处就是静默不一致。

## 方案要点

- 在 `MainWindow` 上抽一个私有 helper（如 `bool IsFileInput()` / `bool IsValidInput()`），三处调用它。
- 保持判定口径与 `UpdateAddSuffixState` 一致（文件夹/空/无效路径 → 非文件输入）。

## Acceptance criteria

- [x] 输入谓词（「这次输入是不是一个文件」）的 `File.Exists` 只剩 helper 内的一处（`IsFileInput`，`rg` 可证）——该文件另 3 处（探测 `models/manifest.conf`、`setting.ini`、引擎清单）是「某个配置文件是否存在」的另一概念，不并入（字面口径按此订正）
- [x] 行为零变化：文件夹输入时下拉灰置显示「开」、文件输入时可点、无效输入仍报参数错误
- [x] `dotnet build gui/ImageUpscaleGui.csproj -c Release` 0 警告 0 错误

## 未决 / 风险

- 纯整洁度，无用户可见影响。
- GUI 无自动化测试（spec 定案）：验收靠源码核对 + 人工点一遍（输入框空/文件夹/文件三态）。

## Comments

### 批次收口（2026-09-20，批次 59 + 60 + 61 + 62 + 63 一次闭环）

- **交付**：`5f0f144`（`IsFileInput` / `IsValidInput` 单一定义来源，输入侧三处调用点收敛到它）。
- **行为零变化的依据**：`!IsValidInput(inp)` 与原 `IsNullOrWhiteSpace || (!File.Exists && !Directory.Exists)` 德摩根等价；
  `--no-rename` 前的 `File.Exists(inp)` → `IsFileInput(inp)` 等值；UIAutomation 逐态 dump（空 / 文件 / 文件夹 / 无效路径）改动前后一致。
- **AC1 口径订正**：票面原文「`File.Exists` 在 `MainWindow.xaml.cs` 中只剩 helper 内的一处」字面不可达（全文件共 4 处），
  另 3 处是「某个配置文件是否存在」（`models/manifest.conf` 探测、`setting.ini`、引擎清单）——**另一概念，不并入**（并入会造出错误抽象）。
  AC 已按票面「方案要点」的原本口径改为「输入谓词的 `File.Exists` 只剩 helper 内一处」。
- **两轴轨迹**：r1 双 `approved` → r2 双 `approved` → **r3 双 `approved`**（两轮都独立提出同一条字面口径问题，均判为改票面而非改代码）。
- **gate**：`dotnet build gui` 0 警告 0 错误 + `pytest tests -q` **100 passed**。
