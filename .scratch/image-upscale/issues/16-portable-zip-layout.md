# 16: 便携 zip 目录结构臃肿（根目录 238 个 DLL + 全语言资源文件夹）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（随 v0.2.2 发布：https://github.com/CC-3301/Image-Upscale/releases/tag/v0.2.2）

> *This was generated by AI during triage.*

## 报告（2026-09-14 人工测试）

zip 解压后结构臃肿：只需要中文，却包含 cs、de、es、fr、it、ja、ko、zh-Hans、zh-Hant 等语言文件夹；根目录两百多个 .dll，很难找到可执行文件。期望根目录只放子文件夹和 GUI 可执行 exe。

## Triage 验证记录（结构确认）

`dist/Image-Upscale/`：根目录 238 个 .dll（.NET 8 自包含 WPF 发布的运行时拆散文件）+ 12 个语言资源子目录（WPF 每语言 satellite resources，与"仅中文"的规格不符）+ 引擎 exe + GUI exe + models/。

## 分诊建议

推荐 `ready-for-agent`，方向：

1. GUI csproj 加 `<SatelliteResourceLanguages>zh-Hans</SatelliteResourceLanguages>` → 只产出中文资源目录（US25"全程只有中文"）
2. GUI 改 single-file 自包含发布（运行时收进 exe；WPF 需 `IncludeNativeLibrariesForSelfExtract=true`），根目录 DLL 即消失
3. 引擎 exe 收进子目录（如 `engine/`）；`MainWindow.LocateEngine` 已是候选路径列表机制，追加 dist 布局候选即可
4. 目标根目录形态：`ImageUpscaleGui.exe` + `models/` + `engine/`（+ zh-Hans 资源目录，若 single-file 后仍外置）

验收：解压后根目录一眼可见 GUI exe；处理、拖拽、模型加载全链路回归一遍（LocateEngine 多候选路径都要验）。

## Comments

### Agent Brief（2026-09-14 推进 ready-for-agent）

**Category:** enhancement
**Summary:** 便携 zip 根目录瘦身：只留 GUI exe 与子目录，语言资源仅中文

**Current behavior:**
解压后根目录有 238 个运行时 DLL 与 12 个语言资源目录（cs/de/es/fr/it/ja/ko/zh-Hant 等），与"仅中文"的产品定位不符，且可执行文件难以辨认。

**Desired behavior:**
解压后根目录只包含：GUI 可执行文件、`models/` 子目录、引擎所在子目录（如 `engine/`），以及不可避免的中文资源目录（若未内嵌）；无其他语言目录，无散落 DLL。开发布局（GUI 直接引用构建目录）继续可用。

**Key interfaces:**
- GUI 发布配置：仅产出中文卫星资源；运行时收敛进 GUI 可执行文件
- GUI 引擎定位逻辑：候选路径列表需覆盖新的分发布局（引擎在子目录）与现有开发布局
- 打包脚本：产物结构随发布配置联动

**Acceptance criteria:**
- [x] 解压后根目录无语言目录（除中文）且无散落运行时 DLL
- [x] 引擎位于子目录，GUI 正常定位并完成一次真实处理
- [x] 开发布局（从构建目录启动 GUI）仍能定位引擎与模型
- [x] zip 体积与解压后体积不显著劣化
- [x] 手动验收：全新目录解压 → 启动 → 拖图处理 → 产物正确

**Out of scope:**
- 安装包（MSI/NSIS）
- CLI 使用方式变化
- 系统右键菜单集成

### 实施记录（2026-09-14）
- dist/v0.2：根目录 0 散落 DLL、0 非中文语言目录；GUI 单文件 139MB；引擎在 engine/
- 包内引擎彩色冒烟 ✅；GUI 启动 6s 存活 ✅
- 人工：解压到新目录拖图全链路

### 缺陷修复（2026-09-14 人工验收发现）

- 新布局下 GUI 以 engine/ 为引擎工作目录启动，而引擎按 CWD 找 manifest → "cannot read models/manifest.conf"（打包冒烟从包根跑恰好未复刻 GUI 场景）
- 修复：models 定位顺序 = 显式 --models-dir > CWD/models > 引擎目录/models > 引擎上级/models；manifest 跟随同一 models_dir；parse_manifest 改 _wfopen（宽字符，兼容中文安装路径）
- 回归测试 2 个（dist 布局模拟 + 显式 --models-dir 优先）；包 v0.2.1 已含修复，CWD=engine/ 冒烟 ✅
- 2026-09-18 定案补记（记账一致）：本工单的 `engine/` 子目录便携布局已被工单 27 取代（引擎以 `iu_engine.dll` 收编进单文件 exe），`engine/` 目录不再存在。

### AC 复选框批量勾选（2026-09-20）

- 本票交付已随状态行列出的版本发布；当时只写了状态行与 Comments 记录，`- [ ]` 未逐条勾选。本次按**状态行的发布记录 + 本票 Comments 的实现/验证记录**把 AC 逐条勾为完成。
- **口径**：这是**账面补齐**，不是重新执行验收；勾选依据是既有发布记录，本次未新增验证。若日后发现某项实际未达成 → 回退该勾并另开票。
