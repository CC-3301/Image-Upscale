# 49: 降噪档位：pro 默认「高」+ 跨模型记忆（齐备档位共用一份，不齐档位各自独立）

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** ready-for-agent

> *This was generated by AI during triage.*

## 报告（维护者，v0.2.8 后）

`realcugan-pro` 的降噪档位默认落在「无」，应为「高」；且降噪档位完全不被记忆。

## 现状（代码确认）

`gui/MainWindow.xaml.cs` 的 `OnModelChanged`：

- `autoOk`（无/低/中/高四档齐备）为假时不加「自动」项，然后一律 `DenoiseBox.SelectedIndex = 0`
  → `realcugan-pro`（只有 `无`/`高`）落到首项「无」而不是「高」。
- 档位是临时选择、不持久化（工单 24 + lessons §2.4 的 grilling 定案）→ 重启/切模型一律回默认。

## 维护者定案（2026-09-20 本轮 triage）

1. **默认档**：档位齐备（无/低/中/高）的模型 → 「自动」；档位不齐的模型 → **最高可用档**
   （`realcugan-pro` = 无/高 → 「高」）；不支持降噪的模型 → 固定「无」并灰置（工单 23 判据不变）。
2. **记忆**：所有支持降噪的模型都记忆档位，但**不按模型各记各的**：
   - 档位齐备的模型（`waifu2x_upconv_7_art` / `waifu2x_cunet` / `waifu2x_upconv_7_photo` /
     `realcugan-se` / `realesr-general-x4v3`）**共用一份全局记忆**：在一个模型上改成「中」，
     切到另一个齐备档位的模型也是「中」。
   - 档位不齐、没有规范 0/1/2/3 档位的模型（当前只有 `realcugan-pro`）**各自独立记忆**。
3. **恢复时校验**：记忆档位不在当前模型的可用集合内 → 回退到该模型的默认档
   （例：全局记忆 = 低，切到只有 无/高 的 pro → 落「高」）。
4. **引擎不动**：`--denoise` 语义与 AUTO 估计器都不改（本次只是 GUI 的默认值与持久化）。

## 方案要点

- `SettingsStore` 新增：
  - `LastDenoise`（`auto` / `none` / `low` / `mid` / `high`，默认 `auto`）—— 全局共用槽。
  - `LastDenoise_<modelId>`（如 `LastDenoise_realcugan-pro`）—— 仅档位不齐的模型各写一个键，
    保存时按已加载模型清单遍历生成，非法值静默回退默认（沿用现有逐项校验语义）。
- `OnModelChanged`：按「齐备 → 全局槽 / 不齐 → 独立槽」取记忆值 → 校验档位存在 → 否则默认档。
  这样 `realcugan-pro` 首次运行落到「高」。
- 选档写回：`DenoiseBox.SelectionChanged` 时把当前档写进对应的记忆槽（内存态），退出时随
  现有 `OnClosing` 一次性落盘。**注意重入**：`OnModelChanged` 内部设置 `SelectedIndex` 会触发
  同一事件，必须用抑制标志包住，否则加载时默认值会覆盖记忆值。

## 变更文件

- `gui/MainWindow.xaml.cs`：`ModelEntry` 加「档位齐备」判据（复用 `Denoise.Keys`）、
  `SettingsStore` 两个槽的读写、`OnModelChanged` 默认/恢复、`DenoiseBox.SelectionChanged` 写回。
- `.scratch/image-upscale/spec.md`：降噪段（AUTO 段第 50 行附近）、GUI 段第 81 行、
  「降噪档位是临时选择」相关表述。
- `CONTEXT.md:44`：`降噪（Denoise）` 词条删掉「不跨模型切换与重启记忆」这句。
- `docs/lessons.md §2.4`：改写「降噪档位是临时选择」这一句（**已获维护者同意改写**）。
- `README.md`：降噪档位说明（第 83 行附近）。
- `.scratch/image-upscale/issues/24-denoise-default-auto.md`：补评论记录定案变更（原文写「不持久化」）。

## Acceptance criteria

- [ ] 首次运行（无 `setting.ini`）：`waifu2x_cunet` → 自动；`realcugan-pro` → 高
- [ ] 齐备档位模型之间切换共用记忆：cunet 选「中」→ 切 `realcugan-se` 也是「中」→ 重启仍是「中」
- [ ] `realcugan-pro` 独立记忆：pro 选「无」→ 切 cunet 仍为全局值 → 切回 pro 仍是「无」
- [ ] 记忆值不可用即回退默认：全局记忆 = 低 → 切到 pro 落「高」
- [ ] `digital-art-4x`（不支持降噪）仍灰置固定「无」，重启后不变
- [ ] `setting.ini` 中可见 `LastDenoise=` 与 `LastDenoise_realcugan-pro=`
- [ ] A（齐备）→ B（不齐）→ A 的切换序列不串槽（不齐模型的选择不污染全局槽）

## 未决 / 风险

- GUI 无自动化测试（spec 定案），本项靠手动验收清单；验收清单条目按上面的 criteria 走。
- 维护者未要求把这些记忆键暴露在界面上，不做设置项展示。
