# 12: 界面各框内文字垂直偏上（应为居左 + 垂直居中）

**Parent:** .scratch/image-upscale/spec.md

**Category:** bug

**Status:** done（随 v0.2.1 发布：https://github.com/CC-3301/Image-Upscale/releases/tag/v0.2.1）

> *This was generated by AI during triage.*

## 报告（2026-09-14 人工测试 + 标注截图）

全部框的文字都往左上偏移；期望是居左 + 居中，实际是居左 + 左上。红圈标注示例：输出格式 ComboBox（"JPG"）。

## Triage 验证记录（截图证据已确认）

放大红圈区域可见：ComboBox 内选中项文字（"JPG"）明显贴近控件顶部而非垂直居中；且控件整体呈经典主题渲染（平面灰、直角、无 Aero2 圆角）——文字偏上是经典主题下控件模板的默认行为。

## 分诊建议

推荐 `ready-for-agent`：

- 对所有文本承载控件显式声明 `VerticalContentAlignment="Center"`（四个 ComboBox、三个 TextBox/输入、两个 Button），不依赖系统主题默认值；`TextBlock` 标签保持 `VerticalAlignment="Center"`
- 顺带审视 28px 固定高度控件的观感一致性（与工单 19 布局重构同批过一遍）

验收：在报告者机器（经典主题渲染环境）上截图对比，ComboBox/Button/TextBox 文字垂直居中。

## Comments

### Agent Brief（2026-09-14 推进 ready-for-agent）

**Category:** bug
**Summary:** 经典主题渲染下控件文字垂直顶对齐，应显式垂直居中

**Current behavior:**
系统以经典主题渲染控件时，ComboBox 等控件的文字贴近控件顶部（已由报告者标注截图确认），观感为"居左+左上"。

**Desired behavior:**
所有承载文字的控件（下拉框、文本框、按钮）文字垂直居中，且不依赖系统主题的默认值；标签文字保持水平居左、垂直居中。

**Acceptance criteria:**
- [ ] 全部 ComboBox、TextBox、Button 显式声明垂直内容居中
- [ ] 标签类 TextBlock 垂直居中不回归
- [ ] 报告者环境（经典主题渲染）截图对比：控件内文字垂直居中

**Out of scope:**
- 整体布局重排与窗口自适应（工单 19）
- 引入第三方主题库

### 实施记录（2026-09-14）
- 隐式样式（ComboBox/TextBox/Button）显式 VerticalContentAlignment=Center
- 人工：经典主题环境下截图对比，控件文字垂直居中
