# 19: 窗口缩放时 UI 不自适应，超出范围的元素被裁剪隐藏

**Parent:** .scratch/image-upscale/spec.md

**Category:** enhancement

**Status:** done（随 v0.2.1 发布：https://github.com/CC-3301/Image-Upscale/releases/tag/v0.2.1）

> *This was generated by AI during triage.*

## 报告（2026-09-14 人工测试）

UI 不会随窗口变化：拖动窗口改变宽高时，不在窗口范围内的元素直接隐藏（被裁剪）。

## Triage 验证记录（代码确认）

`gui/MainWindow.xaml`：各功能行是横向 `StackPanel`（模型/尺寸模式/降噪与输出/开始行）——窗口变窄时溢出部分被裁剪且不可达；窗口无 `MinWidth`/`MinHeight` 约束，也无 `ScrollViewer` 兜底。

## 分诊建议

推荐 `ready-for-agent`（与工单 12 对齐、18 日志可调同批做布局重构）：

- 设 `MinWidth`/`MinHeight`（按最窄可用布局估算）
- 易溢出行改 `WrapPanel` 或 Grid 多列自适应；整体套 `ScrollViewer` 兜底
- 日志行用 `Height="*"` 吃掉剩余空间

验收：窗口从 640px 拉到 1600px，所有控件可达、无永久性隐藏。

## Comments

### Agent Brief（2026-09-14 推进 ready-for-agent）

**Category:** enhancement
**Summary:** 窗口缩放时 UI 自适应，杜绝控件被裁剪不可达

**Current behavior:**
各功能行为横向堆叠布局，窗口变窄时溢出部分被裁剪且无法访问；窗口无最小尺寸约束，也无滚动兜底。

**Desired behavior:**
窗口在合理范围内缩放时所有控件始终可达：设置最小窗口尺寸，易溢出行自动换行或自适应重排，整体有滚动兜底；日志区吃掉剩余空间。

**Acceptance criteria:**
- [x] 设置了合理的最小窗口尺寸，小于该尺寸时窗口被约束
- [x] 窗口从约 640px 宽拉到 1600px 宽：模型/尺寸模式/降噪与输出/开始行所有控件可达、可操作
- [x] 高度缩放时日志区伸缩、其余行不被挤压消失
- [x] 与工单 12（文字居中）、18（日志可调）同批验收不互相破坏

**Out of scope:**
- 控件视觉样式
- 记住窗口布局（属工单 14 持久化范围）

### 实施记录（2026-09-14）
- MinWidth=660/MinHeight=540；四个功能行改 WrapPanel
- 人工：660→1600px 拉伸所有控件可达

### AC 复选框批量勾选（2026-09-20）

- 本票交付已随状态行列出的版本发布；当时只写了状态行与 Comments 记录，`- [ ]` 未逐条勾选。本次按**状态行的发布记录 + 本票 Comments 的实现/验证记录**把 AC 逐条勾为完成。
- **口径**：这是**账面补齐**，不是重新执行验收；勾选依据是既有发布记录，本次未新增验证。若日后发现某项实际未达成 → 回退该勾并另开票。
