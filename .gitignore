# 构建产物
build/
bld/
out/

# 模型文件（经 scripts/fetch-models.ps1 获取，不入库）
models/

# Python 测试
__pycache__/
.venv/
.pytest_cache/
# tests/.baseline/ 不忽略：PSNR 黄金基准图随仓库分发（否则新机器首次跑只能生成后跳过判定）

# 临时产物（统一放仓库根，命名 .tmp-<用途>；含原 .tmp-convert/ 与 .tmp-publish/；见 AGENTS.md）
*.log
.tmp-*

# pi 内部目标文件
.pi/

# asan 构建目录
bld-asan/

# GUI 构建产物
gui/bin/
gui/obj/

# 打包产物
dist/

# 维护者本地测试素材（输入图与对拍产物，不入库）
references/
