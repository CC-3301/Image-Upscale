# Image Upscale

本地图像超分辨率工具（GUI + CLI 同核），定位为 waifu2x-caffe 的现代继任者：不绑死 CUDA 版本，在 RTX 50 系等新显卡上可正常运行。

## Language

**超分（Super-Resolution / SR）**:
用神经网络把低分辨率图像重建为高分辨率图像的过程。
_Avoid_: 放大（泛指任意 resize，无重建语义）

**模型（Model）**:
一份打包好的超分神经网络权重，决定放大倍数、降噪能力与适用图像类型。
_Avoid_: 算法、引擎（引擎指运行模型的程序，两者不同）

**黑白漫画（Manga）**:
以线稿和网点构成的黑/白/灰图像，本工具的第一优先目标图像类型。
_Avoid_: 二次元（过宽，还含彩色插画/游戏截图）

**网点（Screentone）**:
漫画中用规则网状点阵模拟灰阶的印刷手法，超分时必须保持点阵结构而非被抹平。
_Avoid_: 半调（halftone 的音译混用）

**过度涂抹（Over-smoothing）**:
模型把线稿边缘和网点结构抹糊、用生成纹理覆盖原有设计的缺陷，是模型选型的一票否决标准。
_Avoid_: 糊（口语化，含义不定）

**插画（Illustration）**:
数字绘制的彩色图像（彩页插画、游戏截图、壁纸等），区别于网点构成的黑白漫画与相机拍摄的现实照片。
_Avoid_: 数字艺术（那只是 digital-art-4x 模型的专有名称，不是图像类别）

**现实照片（Photo）**:
相机/手机拍摄的连续色调图像，本工具的次要目标，由独立类别的模型处理。
_Avoid_: 真实图像、摄影图

**目标尺寸（Target Size）**:
用户指定的放大后宽度和高度（继承自 waifu2x-caffe 的核心需求）；由内部放大规划（迭代超分至不小于目标，再高质量 resize 到精确值）达成。
_Avoid_: 输出尺寸（含格式与文件大小歧义）

**直通缩放（Direct Resize）**:
目标尺寸小于原图时跳过模型推理、等比缩放直达目标尺寸的处理路径（保持与 waifu2x-caffe 一致：不裁掉画面，输出格式与质量仍按软件设置）。
_Avoid_: 裁剪（crop 会丢画面）、缩略（含义不定）

**降噪（Denoise）**:
在超分的同时压制 JPEG 伪影与扫描噪点的能力，以档位（自动/无/低/中/高；CLI 与引擎称 auto）呈现给用户；自动（AUTO）由伪影启发式估计自动选档，用户手动选档后自动退出。降噪档位跨模型切换与重启记忆：档位齐备（无/低/中/高）的模型共用一份记忆、默认「自动」，档位不齐的模型各自独立记忆、默认最高可用档，不支持降噪的模型固定「无」。
_Avoid_: 去噪（同义，统一用降噪）

**文件添加扩展名（Add File Extension）**:
界面开关，决定**文件输入**的产物名是否带 `-(模型名)-nN-<倍率|尺寸>` 后缀段（直通缩放的 `-(Resize)-<尺寸>` 段同样算）；关时产物 = 源文件同目录 + 原文件名 + 输出格式扩展名，若与源文件同名则原地覆盖源文件（源图被替换、不可撤销）。文件夹输入不生效，固定为开。
_Avoid_: 文件格式扩展名（指 `.jpg/.png/.webp` 这类编码容器后缀，与前者不同义）
